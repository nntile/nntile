name: Build and test

on:
  push:
    branches: [main, graph_api, 'cursor/**']
  pull_request:
    branches: [main, graph_api]
  workflow_dispatch:
  workflow_call:
    inputs:
      is-pull-request:
        required: false
        type: boolean
        default: false

permissions:
  contents: read

env:
  CCACHE_DIR: /tmp/ccache
  PKG_CONFIG_PATH: /opt/starpu/lib/pkgconfig

jobs:
  configure-smoke:
    name: Configure smoke (CMake only)
    runs-on: ubuntu-24.04
    container: {image: ubuntu:24.04}
    defaults:
      run:
        shell: bash
        working-directory: /__w/nntile/nntile
    steps:
      - run: |
          apt-get update
          apt-get install -y --no-install-recommends ca-certificates git
        working-directory: /
      - uses: actions/checkout@v6
      - uses: ./.github/actions/nntile-starpu-setup
      - run: chmod +x .github/scripts/check-nntile-includes.sh
      - run: .github/scripts/check-nntile-includes.sh .
      - run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo -DUSE_CUDA=OFF \
            -DBUILD_TESTS=OFF -DBUILD_EXAMPLES=OFF -DBUILD_PYTHON_WRAPPERS=OFF

  compile-check:
    name: compile-check-${{ matrix.subsystem }}
    needs: configure-smoke
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        subsystem:
          - kernel
          - starpu
          - tile
          - tile_graph
          - tensor_graph
          - nn_graph
          - runtime
          - module
          - optim
          - io
          - dataset
          - model
    container: {image: ubuntu:24.04}
    defaults:
      run:
        shell: bash
        working-directory: /__w/nntile/nntile
    steps:
      - run: apt-get update && apt-get install -y ca-certificates git
        working-directory: /
      - uses: actions/checkout@v6
      - uses: ./.github/actions/nntile-starpu-setup
      - uses: actions/cache/restore@v4
        with:
          path: /tmp/ccache
          key: ccache-compile-${{ matrix.subsystem }}-${{ github.sha }}
          restore-keys: ccache-compile-${{ matrix.subsystem }}-
      - run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo -DUSE_CUDA=OFF \
            -DNNTILE_COMPILE_CHECK_SUBSYSTEM=${{ matrix.subsystem }} \
            -DBUILD_TESTS=OFF -DBUILD_EXAMPLES=OFF -DBUILD_PYTHON_WRAPPERS=OFF \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build --target nntile_compile_check_${{ matrix.subsystem }}

  test-kernel:
    name: test-kernel
    needs: [configure-smoke, compile-check]
    runs-on: ubuntu-24.04
    container: {image: ubuntu:24.04}
    defaults:
      run: {shell: bash, working-directory: /__w/nntile/nntile}
    steps:
      - run: apt-get update && apt-get install -y ca-certificates git
        working-directory: /
      - uses: actions/checkout@v6
      - uses: ./.github/actions/nntile-ci-container
        with: {python-version: '3.12'}
      - run: |
          mapfile -t _flags < <(.github/scripts/cmake-test-subsystem.sh kernel)
          cmake -S . -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo -DUSE_CUDA=OFF \
            -DBUILD_TESTS=ON -DBUILD_TESTS_CORE=ON "${_flags[@]}"
          cmake --build build -j"$(nproc)"
          cd build && ctest -R tests_nntile_constants -V --output-on-failure

  test-full:
    name: test-full-integration
    needs: [compile-check]
    runs-on: ubuntu-24.04
    container: {image: ubuntu:24.04}
    defaults:
      run: {shell: bash, working-directory: /__w/nntile/nntile}
    steps:
      - run: apt-get update && apt-get install -y ca-certificates git
        working-directory: /
      - uses: actions/checkout@v6
      - uses: ./.github/actions/nntile-ci-container
        with: {python-version: '3.12'}
      - run: |
          export TORCH_PREFIX=$(python3 -c 'import torch; print(torch.utils.cmake_prefix_path)')
          cmake -S . -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo -DUSE_CUDA=OFF \
            -DNNTILE_PRESET=full -DBUILD_TESTS=ON -DBUILD_TESTS_GRAPH=ON \
            -DBUILD_TESTS_CORE=ON -DBUILD_TESTS_PYTORCH=ON \
            -DCMAKE_PREFIX_PATH="${TORCH_PREFIX}" \
            -DBUILD_EXAMPLES=ON
          cmake --build build -j"$(nproc)"
          cd build && ctest -j"$(nproc)" --output-on-failure

  test-python:
    name: test-python
    needs: test-full
    runs-on: ubuntu-24.04
    container: {image: ubuntu:24.04}
    defaults:
      run: {shell: bash, working-directory: /__w/nntile/nntile}
    steps:
      - run: apt-get update && apt-get install -y ca-certificates git
        working-directory: /
      - uses: actions/checkout@v6
      - uses: ./.github/actions/nntile-ci-container
        with: {python-version: '3.12'}
      - run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo -DUSE_CUDA=OFF \
            -DNNTILE_PRESET=full -DBUILD_PYTHON_WRAPPERS=ON -DBUILD_TESTS=OFF
          cmake --build build --target nntile_py -j"$(nproc)"
          export PYTHONPATH="${PWD}/build/python"
          python3 -c "import nntile; print('ok', nntile.NNGraph)"
