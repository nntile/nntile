if (SYSTEM_FXT)
    find_package(PkgConfig REQUIRED)
    pkg_check_modules(FxT REQUIRED IMPORTED_TARGET GLOBAL fxt)
    add_library(fxt::fxt ALIAS PkgConfig::FxT)
else()
    include(fxt-vendored.cmake)
endif()
