if (SYSTEM_STARPU)
    find_package(PkgConfig REQUIRED)
    pkg_check_modules(StarPU REQUIRED IMPORTED_TARGET GLOBAL starpu-1.3)
    add_library(starpu::starpu ALIAS PkgConfig::StarPU)
else()
    include(starpu-vendored.cmake)
endif()
